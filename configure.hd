#!/bin/bash
# configure script to workaround homebrew's 118 character line length limit
#
# $1 prefix
# $2 mandir
./configure --prefix=$1 --mandir=$2 --with-ace=/usr/local/include --with-ace-libs=/usr/local/lib

