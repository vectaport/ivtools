name: Send Merge Summary to Self
on:
  pull_request:
    types: [closed]
    branches:
      - master
jobs:
  send-email:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Send Email Summary
        uses: dawidd6/action-send-mail@v3
        with:
          # Google Workspace Server Routing
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          
          # Outbound Content Configuration
          subject: "[ivtools Patch Merged] #${{ github.event.pull_request.number }}: ${{ github.event.pull_request.title }}"
          body: |
            A new patch has been successfully merged into ivtools!
            
            PR TITLE: ${{ github.event.pull_request.title }}
            MERGED BY: ${{ github.actor }}
            URL: ${{ github.event.pull_request.html_url }}
            
            --------------------------------------------------
            CHANGES SUMMARY:
            --------------------------------------------------
            ${{ github.event.pull_request.body }}
            
          # Target Distribution & Routing
          # was: ivtools-patch@lists.sourceforge.net
          # SourceForge mailing list moderation/admin became unworkable
          # (20-year-old Mailman instance, likely zero active subscribers
          # due to spam attrition).  Redirecting to self for now until a
          # lighter-weight notification mechanism (GitHub Discussions, RSS)
          # is decided on.
          to: <${{ secrets.MAIL_USERNAME }}>
          from: GitHub Robot <${{ secrets.MAIL_USERNAME }}>
