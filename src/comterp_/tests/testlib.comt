// src/comterp_/tests/testlib.comt -- shared test utilities
//
// Usage: run("testlib.comt") at the top of each test script
// Provides: prev_ok, check_fail()

prev_ok=true

check_fail=func(
  if(!ok && prev_ok :then print("    ^^^ FIRST FAIL ^^^\n"));
  prev_ok=ok)
