XCOMM
XCOMM include files for using the InterViews library
XCOMM

PACKAGE = include_interviews

IvmkcmTargets($(PACKAGE))

InstallIncludes(InterViews)

XCOMM install::
XCOMM	MakeDir($(INCDIR)/InterViews)
XCOMM	$(INSTALL) -c $(INSTINCFLAGS) *.h $(INCDIR)/IInterViews


